package org.example.backendpfe.repository;

import org.example.backendpfe.Model.ArchivedFormation;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ArchivedFormationRepository extends JpaRepository<ArchivedFormation, Long> {}
